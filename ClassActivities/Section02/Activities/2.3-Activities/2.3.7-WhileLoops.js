// ? TO DO: create a while loop that TOTALS the numbers from 1 to 100 // create a variable to represent the current number
let a = 1;
// create a variable to represent the current total
let total = 0;
// write a while loop that sums the numbers from 1 to 100
while (a <= 100) {
    total += a;
    a++;
    console.log(total);
}

console.log(total);

